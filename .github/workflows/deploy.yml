name: Deploy Property Manager to Dev Environment

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev

    env:
      DB_URL: ${{ secrets.TANTE_MAIN_DB_URL }}
      DB_USERNAME: ${{ secrets.PROPERTY_MANAGER_DB_USERNAME }}
      DB_PASSWORD: ${{ secrets.PROPERTY_MANAGER_DB_PASSWORD }}
      SERVER_PORT: 8082

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Build
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker buildx build --platform linux/amd64 -t property-manager:dev --load .

      - name: Save Docker image as tar
        run: docker save property-manager:dev | gzip > property-manager.tar.gz

      - name: Copy image to EC2
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          source: "property-manager.tar.gz"
          target: "/home/ubuntu/property-manager"

      - name: Deploy on EC2
        run: |
          echo "${{ secrets.EC2_PRIVATE_KEY }}" > private_key
          chmod 600 private_key

          ssh -o StrictHostKeyChecking=no -i private_key \
          ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} \
          "DB_URL='${{ secrets.DB_URL }}' \
           DB_USERNAME='${{ secrets.DB_USERNAME }}' \
           DB_PASSWORD='${{ secrets.DB_PASSWORD }}' \
           bash -s" << 'EOF'

          set -e

          cd /home/ubuntu/property-manager

          echo "Stopping old container..."
          sudo docker stop property-manager || true
          sudo docker rm property-manager || true

          echo "Loading new image..."
          gunzip -c property-manager.tar.gz | sudo docker load

          echo "Starting new container..."
          sudo docker run -d \
            --name property-manager \
            --restart always \
            -p 8082:8082 \
            -e DB_URL="$DB_URL" \
            -e DB_USERNAME="$DB_USERNAME" \
            -e DB_PASSWORD="$DB_PASSWORD" \
            property-manager:dev

          EOF

          rm private_key

